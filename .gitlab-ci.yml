
build:
  stage: build
  image: amd64/debian:bookworm
  script:
    - apt-get update && apt-get -fuy install git default-jdk gradle
    - ./gradlew localDist
  artifacts:
    paths:
      - build/dist
    expire_in: 6 month
